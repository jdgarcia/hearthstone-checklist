var CardConstants = {
  actions: {
    ADD_ONE: 'actions_card_add_one',
    SUBTRACT_ONE: 'actions_card_subtract_one'
  },

  events: {
    CHANGE: 'events_card_change'
  }
};

module.exports = CardConstants;
